services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: devblog-frontend:local
    ports:
      - "5173:5173"
    environment:
      VITE_API_URL: http://localhost:5000
    volumes:
      - .:/app
      - /app/node_modules
